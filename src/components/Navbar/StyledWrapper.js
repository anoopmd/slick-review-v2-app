import styled from 'styled-components';

const StyledWrapper = styled.div`
  .logo-text {
    margin-bottom: -5px;
  }

  nav.navbar {
    max-width: 1024px;
    margin: 0 auto;
  }
`;

export default StyledWrapper;
